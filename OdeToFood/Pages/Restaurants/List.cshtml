@page 
@model ListModel
@{ }

<h1>Restaurants</h1>

<form method ="get" >
    <div class= "form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm"/>
                <span class="input-group-btn">
                    <button class="btn btn-default">
                        <i class = "glyphicon glyphicon-search"></i>
                    </button>
                </span>
        </div>
    </div>
 </form>


<table class="table">
@foreach (var restaurant in Model.Restaruants)
{
    <tr>
        <td>@restaurant.Name </td>
        <td>@restaurant.location </td>
        <td>@restaurant.Cuisine </td>

        <td>
            <a class="btn btn-lg" asp-page="./Detail" asp-route-restaurantId="@restaurant.Id">
                <!--a class="btn btn-lg" herf="/Restaurant/Detail?restaurantId=@restaurant.Id ">-->

                <i class="glyphicon glyphicon-zoom-in"></i>
            </a>
        </td>

        <td>
            <a class="btn btn-lg" asp-page="./Edit" asp-route-restaurantId="@restaurant.Id">
                <!--a class="btn btn-lg" herf="/Restaurant/Detail?restaurantId=@restaurant.Id ">-->

                <i class="glyphicon glyphicon-edit"></i>
            </a>
        </td>

    </tr>
}

</table>

<a asp-page=".\Edit" class="btn btn-primary">Add New</a>

@Model.Message
@Model.MessageAppConfig

